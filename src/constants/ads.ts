/**
 * AppLovin MAX keys — this is not Google AdMob.
 * Get them at https://dash.applovin.com
 *   SDK key: Account → Keys
 *   Banner units: MAX → Ad Units → create Banner for iOS and Android (bundle com.bhaktipath.app)
 */
export const AppLovinConfig = {
  sdkKey: '',
  iosBannerUnitId: '',
  androidBannerUnitId: '',
} as const;
